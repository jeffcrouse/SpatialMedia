#pragma once

#include "ofMain.h"

// include TSPS receiver

// enumerate possible face states, including "never seen", "saw face", and "lost face"

class testApp : public ofBaseApp{

	public:
		void setup();
		void update();
		void draw();
    
        // set up TSPS Receiver
    
        // declare current face state
    
        // load a font for displaying feedback
    
        // ofColor for randomizing background
};
